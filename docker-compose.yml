version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: kontrollka_db
    restart: always
    environment:
      POSTGRES_DB: kontrollka
      POSTGRES_USER: kontrollka
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - kontrollka_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kontrollka"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:alpine
    container_name: kontrollka_redis
    restart: always
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - redis_data:/data
    networks:
      - kontrollka_network
    command: redis-server --appendonly yes

  kontrollka:
    build: .
    container_name: kontrollka
    restart: always
    ports:
      - "5000:5000"
    #Добавляем порты, если нужно несколько воркеров, например "5001:5001"
    environment:
      # Основные
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=postgresql://kontrollka:${DB_PASSWORD}@postgres:5432/kontrollka
      - REDIS_URL=redis://redis:6379
      - HOST=0.0.0.0
      - PORT=5000

      # Аутентификация
      - AUTH_MODE=${AUTH_MODE:-local}

      # LDAP
      - LDAP_SERVER=${LDAP_SERVER}
      - LDAP_DOMAIN=${LDAP_DOMAIN}
      - LDAP_BASE_DN=${LDAP_BASE_DN}
      - LDAP_BIND_USER=${LDAP_BIND_USER}
      - LDAP_BIND_PASSWORD=${LDAP_BIND_PASSWORD}
      - AD_GROUP_ADMIN=${AD_GROUP_ADMIN}
      - AD_GROUP_OPERATOR=${AD_GROUP_OPERATOR}
      - AD_GROUP_VIEWER=${AD_GROUP_VIEWER}

      # Учетные данные
      - DEVICE_USERNAME=${DEVICE_USERNAME:-admin}
      - DEVICE_PASSWORD=${DEVICE_PASSWORD:-admin}
      - DEVICE_ENABLE=${DEVICE_ENABLE}
      - SERVER_USERNAME=${SERVER_USERNAME:-root}
      - SERVER_PASSWORD=${SERVER_PASSWORD}
      - SERVER_KEY_FILE=${SERVER_KEY_FILE}

      # Gunicorn
      - GUNICORN_WORKERS=${GUNICORN_WORKERS:-4}

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./certs:/app/certs:ro
      - ./ssh_keys:/app/ssh_keys:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - kontrollka_network

  celery-worker:
    build:
      context: ./ansible-worker
      dockerfile: dockerfile
    container_name: kontrollka_celery_worker
    restart: always
    deploy:
      mode: replicated
      replicas: ${CELERY_WORKERS:-3}
    command: celery -A celery_app worker --concurrency=${CELERY_CONCURRENCY:-5} --loglevel=info
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DEVICE_USERNAME=${DEVICE_USERNAME:-admin}
      - DEVICE_PASSWORD=${DEVICE_PASSWORD:-admin}
    volumes:
      - ./ansible-worker:/app
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - kontrollka_network
volumes:
  postgres_data:
  redis_data:

networks:
  kontrollka_network:
    driver: bridge